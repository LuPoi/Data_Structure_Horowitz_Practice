# Create an ADT, Set. Use the standard mathematical definition and include the following operations:
# Create, Insert, Remove, IsIn, Union, Intersection, Difference

ADT Set is
	objects:
		a collection contains subrange of integers from 0 to maximum integer (MAXINT) on the computer
	functions:
		for all i ∈ (0, MAXINT); TRUE, FALUSE ∈ Boolean

		Create(): Set
